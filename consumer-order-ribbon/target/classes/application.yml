server:
  port: 8900
spring:
  application:
    name: consumer-order-ribbon
user:
  url: http://localhost:7900/user/

eureka:
  client:
    serviceUrl:
      defaultZone: http://user:123@localhost:8888/eureka
  instance:
    prefer-ip-address: true  # 在 EUREKA 显示IP